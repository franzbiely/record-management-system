import { DomforDirective } from './domfor.directive';

describe('DomforDirective', () => {
  it('should create an instance', () => {
    const directive = new DomforDirective();
    expect(directive).toBeTruthy();
  });
});
